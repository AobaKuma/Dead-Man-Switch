# 霰彈槍彈種切換 (CompAmmoSwitch) 前期規劃

> 範圍：DMS 本體 4 把霰彈類武器 + AncientCorps 的 KS-23K
> 尺度：進取派 — 允許新增 DamageDef 與專用彈丸類
> 撰寫日期：2026-08-18

---

## 1. 現況盤點

| 武器 | 檔案 | 口徑/定位 | 射程 | 連發 | 使用者 | 現有 AmmoSwitch |
|---|---|---|---|---|---|---|
| `DMS_Revolver` 鎮暴左輪 | CivilianGun.xml:282 | 20 ga 側手槍 | 12.9 | 1 | 人類 / `DMS_MechWeaponLight` | ❌ 無（`Verb_Shoot`，固定 `DMS_Bullet_RiotRevolver` 豆袋） |
| `DMS_RiotShotgun` 鎮暴霰彈槍 | CivilianGun.xml:393 | 4 ga 長槍 | 19.9 | 1 | 人類 / `DMS_MechWeaponShotgun` | ✅ 豆袋(預設) / 鹿彈 |
| `DMS_AssaultShotgun` 機兵突擊霰彈槍 | MilitaryGun.xml:24 | 12 ga 全自動 | 14.9 | 3 | 機兵 (`LightAutomatroid`) | ✅ 原版鹿彈(預設) / 12ga 獨頭 |
| `DMS_ShotgunGunMounted` CIDS | MilitaryGun_Mount.xml:98 | 多管近迫 | 12.9 | 3 | 載具型機兵（不可更換） | ❌ 無 |
| `DMS_KS23K` 圖拉戰鬥霰彈槍 | AC / MilitaryGun.xml:342 | 23mm 彈匣供彈 | 18.9 | 2 | 人類可用（AverageHumanCanUse） | ❌ 無（固定 `Bullet_DMS_CombatShotgun`，AP 0.7 實心獨頭） |

**現成資產**

- 圖示（`_Fortified-Framework/Textures/UI/Ammoswitch/`，跨 mod 由 ContentFinder 抓）：
  `12Gauge_BeanBag`、`12Gauge_Breaching`、`12Gauge_Normal`、`Shotgun_Slug`、`Shotgun_Pellet`、`Shotgun_Rubber`、`Shotgun_Dragon`、`Shotgun_EMP`
  → **這 8 張正好就是本規劃需要的全部圖示，不需要新美術。**
- `DMS_Damage_Demolition`（`buildingDamageFactorImpassable 12`）可直接當破門/破障彈的傷害型別。
- `Fortified.Verb_Shoot_AmmoSwitch` 已存在（人類可用的槍要用它，否則不累積射擊經驗）。
- `DMS_BaseSwitchableBullet`（帶 `CompProperties_SwitchableAmmo`）是所有可切換彈丸的父 def。

---

## 2. 設計原則

### 2.1 彈種軸線（每一種彈只回答一個戰術問題）

| 軸線 | 回答的需求 | 代表彈種 |
|---|---|---|
| A 非致命 | 抓俘虜、平息暴動、不想殺 | 豆袋彈、橡膠彈 |
| B 泛用殺傷 | 近距離撂倒無甲/輕甲肉體 | 鹿彈 |
| C 遠距穿甲 | 打有甲目標、拉長交戰距離 | 獨頭彈、鎢芯飛鏢彈 |
| D 反機械電子 | 癱瘓機械單位而非擊毀 | EMP 散彈 |
| E 反工事 | 破門、拆牆、清路 | 破門彈 |
| F 區域拒止 | 蟲潮、走廊、逼退近戰衝鋒 | 龍息彈 |
| G 壓制致盲 | 中斷敵方射擊、掩護突入 | 震撼彈 |

### 2.2 分工原則

- **不讓任何一把槍拿到全部軸線。** 每把槍只給 3–4 個選項，其中 1 個是它的本職，其餘是「換個問題也還能解」。
- **代價必須看得見。** `AmmoOption` 只有 `warmUpFactor` / `accuracyFactor` / `rangeFactor` / `burstShotCountOverride` 四個修正，傷害與穿甲差異全部寫在彈丸 def 上。任何一個彈種若在所有面向都不輸預設彈，就是設計失敗。
- **切換成本分級**：側手槍 `switchCooldown 90`、長槍 `120`、載具/多管 `180`。
- **`includeDefaultAmmo` 建議一律設 `false`**，讓每個選項都有具名 label / description / icon；現有兩把把原版 `Bullet_Shotgun` 當隱式預設，會在選單裡出現一個沒有敘述的「預設彈藥」，應一併補成具名選項。

---

## 3. 各槍彈種配置

### 3.1 `DMS_Revolver` 鎮暴左輪 — 「什麼都能應付一點」的警備側手槍

定位：早期（Gunsmithing + DMS_EquipmentBasis）就能拿到的萬用側手槍。敘述本身就寫「可同時應付大多數敵人類型」，切換彈種是把這句話兌現。

| # | 彈種 | 軸線 | damageDef / 數值方向 | 修正 | 圖示 | 需求 |
|---|---|---|---|---|---|---|
| 0 | 20 ga 豆袋彈（現成 `DMS_Bullet_RiotRevolver`） | A | Blunt 16 / AP 0.20 | — | `12Gauge_BeanBag` | 抓俘虜、鎮壓囚犯暴動 |
| 1 | 20 ga 鹿彈 | B | Bullet 18 / AP 0.15 | `accuracyFactor 0.9` | `Shotgun_Pellet` | 突然要見血時 |
| 2 | 20 ga 獨頭彈 | C | Bullet 22 / AP 0.35 / speed 65 | `rangeFactor 1.25`、`accuracyFactor 1.1`、`warmUpFactor 1.15` | `Shotgun_Slug` | 對付輕型機蟲、稍遠的目標 |

新增彈丸：2 個。彈丸 def 要改掛 `DMS_BaseSwitchableBullet`（現有的 `DMS_Bullet_RiotRevolver` 掛的是 `BaseBullet`，要換父）。
verbClass：`Verb_Shoot` → `Fortified.Verb_Shoot_AmmoSwitch`。

---

### 3.2 `DMS_RiotShotgun` 鎮暴霰彈槍 — 「突入小組的槍」

定位：人類手上唯一的攻堅平台。單發、射程 19.9、解鎖在 `DMS_MechBasis`（機甲齒輪），敘述明講是為了「鎮暴與癱瘓敵對機兵」。把它做成執法/攻堅套件，穿甲主線讓給軍用槍。

| # | 彈種 | 軸線 | damageDef / 數值方向 | 修正 | 圖示 | 需求 |
|---|---|---|---|---|---|---|
| 0 | 4 ga 豆袋彈（現成） | A | Blunt 20 / AP 0.24 | — | `12Gauge_BeanBag` | 非致命制伏 |
| 1 | 4 ga 鹿彈（現成） | B | Bullet 24 / AP 0.20 | `accuracyFactor 0.95` | `Shotgun_Pellet` | 一般殺傷 |
| 2 | 4 ga EMP 散彈 | D | `EMP` 傷害 12、`explosionRadius 1.2` 小範圍 | `rangeFactor 0.75`、`accuracyFactor 0.9` | `Shotgun_EMP` | 癱瘓機兵/機械蟲，配合近戰或俘獲 |
| 3 | 4 ga 破門彈 | E | `DMS_Damage_Demolition` 35、`explosionRadius 0.9` | `rangeFactor 0.4`、`warmUpFactor 1.2` | `12Gauge_Breaching` | 貼臉炸門、拆薄牆、開突入口 |

新增彈丸：2 個（EMP 散彈用 `Projectile_Explosive`；破門彈同）。
`explosionRadius` 刻意壓到 1 格以下，避免變成手持迫砲；破門彈的 `rangeFactor 0.4` → 實際約 8 格，強迫貼上去用。

---

### 3.3 `DMS_AssaultShotgun` 機兵突擊霰彈槍 — 「近距離火力承擔者」

定位：機兵手上的三連發近戰壓制槍，`DMS_DeadMansSwitch` 解鎖。這把是唯一同時有「連發」與「彈種」兩個維度可調的槍，`burstShotCountOverride` 應該在這裡用滿。

| # | 彈種 | 軸線 | damageDef / 數值方向 | 修正 | 圖示 | 需求 |
|---|---|---|---|---|---|---|
| 0 | 12 ga 鹿彈（原版 `Bullet_Shotgun`，改為具名選項） | B | Bullet 18 / AP 0.15 | — | `12Gauge_Normal` | 標準壓制 |
| 1 | 12 ga 獨頭彈（現成） | C | Bullet 30 / AP 0.35 | `rangeFactor 1.6`、`accuracyFactor 1.1`、`warmUpFactor 1.15`、`burstShotCountOverride 1` | `Shotgun_Slug` | 拉開距離、打輕甲 |
| 2 | 12 ga 鎢芯飛鏢彈 | C | Bullet 12 / AP 0.55 / speed 90 | `burstShotCountOverride 5`、`accuracyFactor 0.85`、`rangeFactor 0.9` | `Shotgun_Pellet`（或沿用 `12Gauge_Normal` 加色） | 對重甲機兵潑穿甲彈幕；單發傷害低，靠彈量堆 |
| 3 | 12 ga 龍息彈 | F | `Flame` 10、`ai_IsIncendiary true` | `rangeFactor 0.55`、`burstShotCountOverride 4` | `Shotgun_Dragon` | 燒退蟲潮、封走廊、對付近戰衝鋒 |

新增彈丸：2 個（飛鏢彈、龍息彈）。
※ 飛鏢彈與獨頭彈都在 C 軸線但手感相反（一發遠距重擊 vs 五發近距穿甲彈幕），若嫌重疊可先做龍息彈，飛鏢彈列為第二批。

---

### 3.4 `DMS_ShotgunGunMounted` (CIDS) — 「載具的近迫自衛」

定位：象龜等載具型機兵的固定武裝，玩家不能換槍，所以彈種切換是它唯一的戰術彈性。全部選項維持短射程、多發，**不給獨頭彈**（多管滑膛設定上打不了獨頭），與突擊霰彈槍區隔開。

| # | 彈種 | 軸線 | damageDef / 數值方向 | 修正 | 圖示 | 需求 |
|---|---|---|---|---|---|---|
| 0 | 近迫鹿彈（原版 `Bullet_Shotgun`，具名化） | B | Bullet 18 / AP 0.15 | — | `12Gauge_Normal` | 預設反人員 |
| 1 | 燃燒散彈 | F | `Flame` 12、`ai_IsIncendiary true` | `rangeFactor 0.7`、`burstShotCountOverride 5` | `Shotgun_Dragon` | 蟲潮貼臉時的最後一道防線 |
| 2 | EMP 霰彈 | D | `EMP` 10、`explosionRadius 1.0` | `rangeFactor 0.85`、`burstShotCountOverride 2` | `Shotgun_EMP` | 反敵對機械近迫突擊 |

新增彈丸：2 個（可與 3.2 / 3.3 共用同一顆 EMP 散彈與龍息彈丸，只靠 `AmmoOption` 的修正做出差異 → **實際新增可壓到 0 個**，建議共用）。
掛載型武器由 `Pawn_EquipmentTracker.GetGizmos` 的 patch 提供按鈕，載具機兵一樣看得到切換 gizmo，已驗證可行。
`switchCooldown 180`（載具換彈鏈比較慢）。

---

### 3.5 `DMS_KS23K` 圖拉戰鬥霰彈槍（AncientCorps）— 「照現實 KS-23 彈藥表做」

定位：古兵團的 23mm 破障霰彈槍，預設彈 AP 0.7、射程 18.9、雙連發。現實中 KS-23 的整套制式彈藥本身就是一份完美的彈種表，直接照抄最符合本模組的軍事寫實調性。

| # | 彈種 | 現實對應 | 軸線 | damageDef / 數值方向 | 修正 | 圖示 | 需求 |
|---|---|---|---|---|---|---|---|
| 0 | 「路障」實心獨頭彈（現成 `Bullet_DMS_CombatShotgun`） | Barrikada | C | Bullet 24 / AP 0.70 | — | `Shotgun_Slug` | 打車體、機兵裝甲、掩體後 |
| 1 | 「彈片-25」鹿彈 | Shrapnel-25 | B | Bullet 16 / AP 0.15 | `burstShotCountOverride 3`、`rangeFactor 0.7`、`accuracyFactor 0.9` | `Shotgun_Pellet` | 近距離對無甲群體 |
| 2 | 「波浪-R」橡膠彈 | Volna-R | A | Blunt 22 / AP 0.10 | `rangeFactor 0.6` | `Shotgun_Rubber` | 非致命抓俘、驅散 |
| 3 | 「星辰」震撼彈 | Zvezda | G | 新 `DMS_Damage_Flashbang`（0 傷害 + 短暫暈眩/致盲）、`explosionRadius 1.5` | `rangeFactor 0.75`、`burstShotCountOverride 1`、`warmUpFactor 1.2` | `12Gauge_Breaching` | 中斷敵方射擊、掩護突入 |

新增彈丸：3 個。新增 DamageDef：1 個（`DMS_Damage_Flashbang`）。
※ 這把在 AncientCorps，**要在 AC 自己的檔案裡改**，不要寫進本體。
※ 震撼彈是唯一需要新機制的項目：RimWorld 沒有通用致盲，實作方式建議 `Projectile_Explosive` + `postExplosionGasType BlindSmoke`（1 格）+ 對機械同時給 `Stun`。若想先上線，可把 index 3 延後到第二批。

---

## 4. 資源盤點總表

| 項目 | 數量 | 備註 |
|---|---|---|
| 新彈丸 ThingDef | 9（共用 EMP / 龍息後可降到 **7**） | 全部 `ParentName="DMS_BaseSwitchableBullet"` |
| 既有彈丸改父 def | 1（`DMS_Bullet_RiotRevolver`） | `BaseBullet` → `DMS_BaseSwitchableBullet` |
| 新 DamageDef | 1（`DMS_Damage_Flashbang`） | 可延後 |
| 新 UI 圖示 | **0** | FFF 現有 8 張全部命中 |
| 新掛 `CompProperties_AmmoSwitch` 的槍 | 3（Revolver / CIDS / KS-23K） | |
| 改 verbClass 的槍 | 2（Revolver → `Verb_Shoot_AmmoSwitch`；KS-23K 同） | 人類可用槍必須換，否則不給射擊經驗 |
| DefInjected 條目 | 17 選項 × 2 欄 × 3 語 = **102 行** | 索引路徑，見 §5 |

---

## 5. 實作注意事項（踩過的雷）

1. **DefInjected 只能用數字索引**
   `<DefName>.comps.CompAmmoSwitch.ammos.<index>.label` / `.description`。
   `AmmoOption` 沒有 `[TranslationHandle]` 欄位，用名字當 handle 會失效。插入或重排彈種時，三語檔的索引都要一起改。
2. **Def XML 的 label/description 一律寫英文當基準**，繁中/簡中走 DefInjected；`<li>` 欄位順序固定 `label → description → iconPath → projectileDef → 各種 factor`。
3. **CE 相容**：`1.6/CE/Patches/Weapons/AmmoSwitch_CE_Remove.xml` 要補上 `DMS_Revolver`、`DMS_ShotgunGunMounted`；KS-23K 則要在 AncientCorps 自己的 CE patch 加。否則裝 CE 的玩家會看到一顆按了沒反應的按鈕。
4. **`Bullet_Shotgun` 是原版 def**，沒有 `CompProperties_SwitchableAmmo`。要具名化成 index 0 時，建議改用 `useDefaultProjectile true` 的選項（保留 verb 的預設彈），而不是直接指向原版彈丸。
5. **`explosionRadius` 要壓在 1.5 以下**。霰彈槍的爆炸彈種一旦半徑做大，就變成手持迫砲，會吃掉無後座力砲的定位。
6. **平衡自檢**：每個新彈種寫完後，逐條問「它在哪個面向比預設彈差？」答不出來就是數值有問題。

---

## 6. 建議實作批次

| 批次 | 內容 | 理由 |
|---|---|---|
| 第一批 | RiotShotgun 補 EMP + 破門彈；AssaultShotgun 補龍息彈；既有 4 個選項補 label / description / iconPath | 純現成機制，改完即可上線，也順便清掉 memory 裡「尚未有 label/icon」的待辦 |
| 第二批 | Revolver（3 彈種，含改父 def 與 verbClass）；CIDS（共用第一批的 EMP / 龍息彈丸） | 新掛 comp，需要同步改 CE patch |
| 第三批 | KS-23K 全套 4 彈種 + `DMS_Damage_Flashbang`；AssaultShotgun 的鎢芯飛鏢彈 | 跨 mod + 需要新機制，風險最高，放最後 |
