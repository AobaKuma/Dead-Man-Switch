# DMS 軍官受訓晉升任務（DMS_OfficerTraining）設計

> 定位：把皇權「授銜典禮」的掛接方式，接到 DMS 軍事法庭那套「穿梭機接走 → 外借 pawn → 訊號鏈結算 → 空投歸還」的骨架上。
> 玩家決策：用一段真空期（少一個高階戰力 N 天）＋一段外交風險，換一次免典禮的晉升與一筆大額經驗。

---

## 0. 已定案的規格

| 項目 | 決定 |
|---|---|
| 觸發 | **取代授銜典禮**：當 pawn 即將獲得的階級是 `DMS_WarrantOfficer` 或 `DMS_Major` 時，改跑受訓任務；其餘階級照舊跑 `DMS_PromotionCeremony` |
| 課程科目 | **任務生成時就決定並寫進敘述**（射擊／近戰／社交／知識四選一），玩家可據此挑人送訓 |
| 失敗被俘 | pawn 進**艦隊的綁架名單**（`KidnappedPawnsTracker`），日後可透過原版贖金要求／攻打據點救回 |
| 完訓報酬 | 晉升 ＋ 課程對應技能大量經驗 ＋ 隨機移除一個負面特質 |

> **命名待確認**：問答中寫的是「士兵到士官、士官到將官」，但指定的 def 是 `DMS_WarrantOfficer`（准尉，seniority 200）與 `DMS_Major`（少校，400）。實作以 **def 名為準**；若原意是 `DMS_Sergeant`（中士，100），改一行 XML 即可（見 §2）。

---

## 1. 階級階梯與插入點

```
Reservist 0 → Private 10 → Corporal 50 → PreSergeant 75 → Sergeant 100
  → ★ WarrantOfficer 200 → Lieutenant 250 → Captain 300
  → ★ Major 400 → Colonel 500 → Brigadier 1000
```

★ 兩個門檻走受訓任務，其餘走典禮。等於一場戰役生涯裡只會遇到兩次「被送上艦隊」的節點，稀有度剛好，也不會讓受訓變成刷經驗的常態管線。

---

## 2. 掛接方式：改寫既有的 Harmony Prefix

現有 `Patch_GenerateBestowingCeremonyQuest` 已經攔截 `RoyalTitleUtility.GenerateBestowingCeremonyQuest(pawn, faction)`，只要在裡面多一個岔路：

```csharp
if (faction.def != DMS_DefOf.DMS_Army) return true;   // 非艦隊，交還原版

RoyalTitleDef next = pawn.royalty.GetTitleAwardedWhenUpdating(faction, pawn.royalty.GetFavor(faction));
bool wantsTraining = next?.GetModExtension<TitleTrainingExtension>() != null;

QuestScriptDef script = wantsTraining ? DMS_DefOf.DMS_OfficerTraining
                                      : DMS_DefOf.DMS_PromotionCeremony;
// …（原本的 slate 設定 + CanRun + GenerateQuestAndMakeAvailable 流程不變）
return false;
```

**用 `DefModExtension` 而不是在 C# 寫死階級清單**，讓「哪些階級要受訓」變成純資料：

```xml
<!-- 1.6/Royalty/Defs/Title/DMS_Titles.xml，加在 DMS_WarrantOfficer / DMS_Major 上 -->
<modExtensions>
  <li Class="DMS.TitleTrainingExtension">
    <trainingDaysRange>15~20</trainingDaysRange>
    <skillXp>35000</skillXp>
    <passionUpgradeChance>0.25</passionUpgradeChance>
  </li>
</modExtensions>
```

好處：改插入點＝改 XML；別的 mod／相容補丁也能自行掛。

### 防重複觸發

`GenerateBestowingCeremonyQuest` 會被反覆呼叫（榮譽再漲一點就再叫一次）。受訓中的 pawn 不在地圖上，典禮任務會 `CanRun` 失敗，但受訓任務的 `TestRunInt` 必須自己擋：

- 掃 `Find.QuestManager.QuestsListForReading`，若已有 `root is QuestNode_Root_OfficerTraining` 且狀態為 `NotYetAccepted / Ongoing`、且 slate 的 `trainee` 是同一個 pawn → 回 false。

---

## 3. 生成期（`QuestNode_Root_OfficerTraining.RunInt`）

### 3.1 前置檢查（`TestRunInt`）

1. `ModsConfig.RoyaltyActive`，`transportShipDef?.shipThing != null`
2. 艦隊存在且 **不敵對**（`!fleet.HostileTo(Faction.OfPlayer)`）
3. `trainee` 是玩家自由殖民者、在玩家地圖、未倒地、未被囚
4. `GetTitleAwardedWhenUpdating` 帶有 `TitleTrainingExtension`
5. 沒有進行中的同 pawn 受訓任務
6. `map.mapPawns.FreeColonistsSpawnedCount >= 2`（避免把最後一個殖民者送走直接遊戲結束）
7. 能取得 `asker`（艦隊教育訓練處軍官，`mustBeOfKind = DMS_Officer_Ceremonist`）

### 3.2 生成時定案並寫進 slate 的變數

| slate 變數 | 內容 | 用途 |
|---|---|---|
| `trainee` | 受訓 pawn | `[trainee_nameFull]` / `[trainee_pronoun]` 等文法變數 |
| `asker` | 艦隊軍官 | 同上 |
| `issuerFactionName` | 艦隊名 | 敘述 |
| `newTitle` | 目標階級 label | 敘述 |
| `courseName` | 課程名（Keyed 翻譯後的字串） | 敘述 |
| `courseSkill` | 技能 label | 敘述 |
| `trainingDays` | `trainingDaysRange.RandomInRange` | 敘述＋Delay |
| `boardingDeadlineDays` | 2 | 登機期限 |

### 3.3 課程表（四選一，生成時抽）

| 課程 key | 技能 | 繁中 | 英文 |
|---|---|---|---|
| `DMS_Course_Infantry` | Shooting | 步兵戰術課程 | Infantry tactics course |
| `DMS_Course_CQB` | Melee | 近身戰鬥教範 | Close-quarters combat course |
| `DMS_Course_Command` | Social | 指揮與領導學程 | Command and leadership course |
| `DMS_Course_Staff` | Intellectual | 參謀作業學程 | Staff and military science course |

課程清單寫在 `<root>` 節點的 `<courses>` 裡（`List<CourseOption>`：`skill` / `labelKey` / `weight`），要加課程只改 XML。

### 3.4 穿梭機

比照軍事法庭：生成期就 `ThingMaker.MakeThing(transportShipDef.shipThing)`，設定
`requiredPawns = [trainee]`、`acceptColonists = false`，掛 questTag `shuttle`，接受任務時才降落。

---

## 4. 訊號鏈（執行期）

```
inSignal (接受)
 ├─→ QuestPart_SpawnTrainingShuttle       穿梭機降落 + 抵達信件
 ├─→ QuestPart_Delay  登機期限 2 天  ──逾時──→ sigBoardDeadline
 │      inSignalDisable = shuttle.SentSatisfied，isBad = true
 └─→ QuestPart_TrainingWatch (Activable)  每 250 tick 檢查艦隊是否轉敵對

shuttle.SentSatisfied (sigSent)
 ├─→ QuestPart_LendColonistsToFaction     pawn 外借給艦隊
 │      returnLentColonistsInTicks = 1000000000（永不自動歸還）
 │      returnMap = map.Parent
 │      outSignalColonistsDied = sigDied
 │      outSignalsCompleted   = sigReturned
 └─→ QuestPart_Delay  trainingDays  ──→ sigGraduate

sigGraduate
 └─→ QuestPart_TrainingGraduation
        晉升 → 灌經驗 → 移除負面特質 → 完訓信件 → lend.Complete()
        （lend 的 Complete 會空投歸還並發出 sigReturned）

失敗匯流 → sigFail：
   sigBoardDeadline      登機逾時（穿梭機空手飛走）
   shuttle.Destroyed     穿梭機被打掉
   sigDied               受訓期間陣亡（原版 lend part 會把屍體空投回來）
   sigAborted            尚未登機時艦隊轉敵對 → 單純失敗，不俘虜
   sigCaptured           已登機後艦隊轉敵對 → 俘虜

sigReturned → QuestEndOutcome.Success
sigFail     → QuestPart_SendShuttleAway + QuestEndOutcome.Fail
（另掛 QuestPart_SendShuttleAwayOnCleanup 做善後）
```

---

## 5. 三個新 QuestPart 的職責

### 5.1 `QuestPart_SpawnTrainingShuttle`

與 `QuestPart_SpawnCourtShuttle` 幾乎一樣（降落、`ShipJob_Wait{leaveImmediatelyWhenSatisfied=true}`、發抵達信件），差別只有信件文法用哪個 RulePack。

**做法**：把共通邏輯抽成基底 `QuestPart_SpawnPickupShuttle`（帶 `RulePackDef letterPack` 欄位），
讓 `QuestPart_SpawnCourtShuttle : QuestPart_SpawnPickupShuttle` **保留原類別名**（QuestPart 是以類別名存檔的，改名會讓正在跑軍事法庭的存檔壞掉），新任務用 `QuestPart_SpawnTrainingShuttle` 另開一個子類。

### 5.2 `QuestPart_TrainingWatch`（`QuestPartActivable`）

- `Enable`：記下 enable tick。
- `QuestPartTick`：每 250 tick 檢查 `fleet.RelationKindWith(player) == Hostile`。
  沿用 `QuestPart_CourtTruce` 的輪詢寫法，不依賴 `faction.BecameHostileToPlayer` 訊號 —— 輪詢能同時抓到玩家主動挑釁與各種間接翻臉來源。
- 翻臉時分兩種：
  - **pawn 還沒登機**（lend part 尚未 Enabled）→ 發 `sigAborted`，穿梭機飛走，任務失敗，不俘虜。
  - **pawn 已上去了** → 執行俘虜流程，發 `sigCaptured`。

俘虜流程（順序不能顛倒）：

```csharp
// 1. 先把 pawn 從 lend 的清單移除，否則任務清理/後續 Complete 會把人空投回來
lend.LentColonistsListForReading.Remove(trainee);   // 回傳的是同一個 List 參照，可直接改

// 2. 綁架。kidnapper 不能是 null —— KidnappedPawnsTracker.Kidnap 內部
//    直接 kidnapper.Named(...) 會 NRE
fleet.kidnapped.Kidnap(trainee, asker);

// 3. 發俘虜信件，再送 sigFail
```

`Kidnap` 內部已處理「pawn 已在 world pawns」的情況（`if (!Find.WorldPawns.Contains(pawn))`），
並會自動給殖民地「失去同伴」的心情、通知帳單系統、更新遊戲結束判定。

> 注意：**不要**呼叫 `lend.Complete()` 來收尾。`Complete()` 會發出 `sigReturned` 導致任務判定為成功。
> 正確做法是清掉 lend 的清單後直接送 `sigFail`，任務 End 時 activable part 走 `Cleanup()`，不會觸發 `Complete()`。

### 5.3 `QuestPart_TrainingGraduation`

收到 `sigGraduate` 後，依序：

1. **保險**：`trainee == null || trainee.Dead` → 直接送 `sigFail`。
2. **晉升**
   ```csharp
   RoyalTitleDef target = newTitle;
   RoyalTitleDef cur = trainee.royalty.GetCurrentTitle(fleet);
   if (cur != null && cur.seniority >= target.seniority)      // 受訓期間榮譽又漲了
       target = trainee.royalty.GetTitleAwardedWhenUpdating(fleet, trainee.royalty.GetFavor(fleet));
   trainee.royalty.SetTitle(fleet, target, grantRewards: true,
                            rewardsOnlyForNewestTitle: true, sendLetter: false);
   ```
   （簽名：`SetTitle(Faction, RoyalTitleDef, bool grantRewards, bool rewardsOnlyForNewestTitle, bool sendLetter)`；
   榮譽是累積制不用扣，`sendLetter:false` 是因為我們自己發完訓信。）
3. **技能經驗**
   ```csharp
   trainee.skills.GetSkill(courseSkill).Learn(skillXp, direct: true);
   if (無熱情 && Rand.Chance(passionUpgradeChance)) 熱情 +1 級
   ```
   `direct: true` 會略過學習速度倍率與每日經驗上限。
4. **移除一個負面特質**
   - 從 `trainee.story.traits.allTraits` 篩出落在 `<removableTraits>`（`List<TraitRequirement>`，含 def + degree）白名單裡的項目，隨機一個 `RemoveTrait(trait, unsuppressConflicts: true)`。
   - 用**白名單**而不是啟發式判斷，避免誤刪其他 mod 的特質或基因來源特質（`trait.sourceGene != null` 的一律排除 —— `RemoveTrait` 會連基因一起拔掉）。
   - 建議預設白名單：`Wimp`、`Slowpoke`、`SlowLearner`、`Abrasive`、`Volatile`、`Greedy`、`Jealous`、`Pyromaniac`、`Gourmand`、`DislikesMen`、`DislikesWomen`、`Nerves`(degree -1/-2)、`NaturalMood`(degree -1/-2)。
   - **沒有可移除的負面特質時**要有補償，否則好兵反而虧：改為額外 `+50%` 經驗（或 +2 榮譽），並在信件裡改成另一段文案。
5. 發完訓信件 → `lend.Complete()` → 空投歸還 → `sigReturned` → Success。

---

## 6. 數值建議（先落地再實測微調）

| | 准尉課程（WarrantOfficer） | 少校課程（Major） |
|---|---|---|
| 訓練天數 | 15~20 | 25~30 |
| 技能經驗 | 35,000 | 60,000 |
| 熱情升級機率 | 25% | 35% |
| 登機期限 | 2 天 | 2 天 |
| 移除負面特質 | 1 個 | 1 個 |
| 任務過期 | 8~12 天 | 8~12 天 |

換算（原版升級曲線：L→L+1 需要 `1000×(L+1)` 點，累積到 10 級 55,000、15 級 120,000）：

- 35,000 ≈ 把一項 0 級技能推到 7 級；或把 8 級推到約 11 級。
- 60,000 ≈ 把 0 級推到約 10 級；或把 10 級推到約 13 級。

夠明顯，但拉不出一個滿級專家 —— 保留「受訓是起點不是終點」的語氣。

---

## 7. 檔案清單

### C#（`_Source/DMS/Quests/OfficerTraining/`）

| 檔案 | 內容 |
|---|---|
| `QuestNode_Root_OfficerTraining.cs` | 生成、資格檢查、訊號接線 |
| `QuestPart_SpawnTrainingShuttle.cs` | 穿梭機降落（含抽出的 `QuestPart_SpawnPickupShuttle` 基底） |
| `QuestPart_TrainingWatch.cs` | 敵對輪詢 + 俘虜流程 |
| `QuestPart_TrainingGraduation.cs` | 晉升／經驗／特質／歸還 |
| `TitleTrainingExtension.cs` | `DefModExtension`，掛在 RoyalTitleDef 上 |
| `OfficerTrainingText.cs` | RulePack 快取（比照 `CourtMartialText`） |

### C#（既有檔案修改）

- `Royalty/Patch_GenerateBestowingCeremonyQuest.cs` —— 加受訓岔路
- `DMS_DefOf.cs` —— 加 `QuestScriptDef DMS_OfficerTraining`
- `Quests/CourtMartial/QuestPart_SpawnCourtShuttle.cs` —— 改為繼承新基底（**類別名不動**）

### XML

| 路徑 | 內容 |
|---|---|
| `1.6/Royalty/Defs/Quests/OfficerTraining.xml` | `QuestScriptDef DMS_OfficerTraining` ＋ `RulePackDef DMS_OfficerTrainingLetters` |
| `1.6/Royalty/Defs/Title/DMS_Titles.xml` | 兩個 `<modExtensions>` |
| `1.6/Languages/{English,繁,簡}/Keyed/DMS_SupplyChain.xml` | 課程名 4 條 ＋ 期限／進度提示 3 條 |
| `1.6/Royalty/Languages/{繁,簡}/DefInjected/QuestScriptDef/OfficerTraining.xml` | questName / questDescription（各 3 變體） |
| `1.6/Royalty/Languages/{繁,簡}/DefInjected/RulePackDef/OfficerTraining.xml` | 信件文法 |

信件關鍵字（比照 `DMS_CourtMartialLetters`）：
`arrivedLetter*`（穿梭機抵達）、`graduateLetter*`（完訓晉升）、`graduateNoTraitLetter*`（無負面特質可移除）、
`capturedLetter*`（翻臉被俘）、`abortLetter*`（未登機即破局）、`missedLetter*`（登機逾時）。

---

## 8. 敘述文案方向

沿用 DMS 的兩段式軍事公文語氣、段落間用**字面 `\n\n`**、英文寫在 Def 裡、繁中／簡中走 DefInjected（簡中用 `tw2s` 純字形轉換）。英文骨架草稿：

> A training officer of the [issuerFactionName] service education command, [asker_nameFull], has forwarded a course allocation to us. [trainee_nameFull] has accumulated sufficient service credit for the rank of [newTitle], but the fleet does not confer that grade by ceremony — it is conferred at the end of a course.
>
> [asker_pronoun] will requisition an orbital transport to collect [trainee_nameDef] for the [courseName], [trainingDays] days of residential instruction aboard the fleet. Board, quarters and medical care are the fleet's charge. On graduation [trainee_pronoun] returns commissioned as [newTitle], with the course's [courseSkill] instruction on record.
>
> The allocation holds only as long as the peace does. Should [playerFactionName] take up arms against [issuerFactionName] while [trainee_nameDef] is aboard, [trainee_pronoun] will be detained as an enemy combatant and will not be coming back.

---

## 9. 待確認 / 已知風險

1. **插入點命名**（§0）—— 是 `WarrantOfficer/Major`，還是原意的 `Sergeant/…`？
2. **階級硬性需求**：`DMS_WarrantOfficer` 以上會帶 `bedroomRequirements`（20 格、鋪地板、無生產設施）與頭盔 tag 需求。受訓路線不像典禮那樣有王座廳把關（DMS 各階級沒設 `throneRoomRequirements`，所以本來就不擋），晉升後若房間不合格會直接吃心情懲罰。要不要在任務接受條件裡加一條 `QuestPart_RequirementsToAcceptThroneRoom` 等價的房間檢查？（建議：**不加**，改在敘述末尾補一句提醒，維持「受訓路線比較省事」的取捨。）
3. **受訓期間玩家沒得反悔**：pawn 一登機就進 world pawn，沒有中途召回的手段。若需要，可加一個「提前召回」的 quest 選項（付出榮譽或好感度）。
4. **`RemoveTrait` 對未生成 pawn 的安全性**：內部會碰 `pawn.Drawer.renderer`（有圖形屬性的特質）與 `pawn.Map`（已 null 檢查）。世界 pawn 的 `Drawer` 是延遲建立的，理論上安全，但仍建議白名單只放無圖形屬性的特質。
5. **存檔相容**：新增 QuestPart 類別不影響舊檔；唯一風險是 §5.1 的類別改名，已用「保留舊名 + 抽基底」規避。
6. **`Publicizer`**：目前 `.csproj` 已開 `QuestPartActivable.Complete` 與 `QuestPart_LendColonistsToFaction.Complete`，本任務用得到的都已具備，不需新增。
