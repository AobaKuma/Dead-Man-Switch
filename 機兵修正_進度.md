# 機兵一致性修正 — 完成報告

稽核清單（`機兵敘述與數值一致性稽核.md`）中被指派的項目 **全部完成**。
全 mod XML 已通過 well-formed 驗證；機兵 ThingDef 由 36 → 34（移除兩個 `_b` 變體）。

---

## A. 明確矛盾

### A1 壁虎 — 外掛電磁反應裝甲（ERA）
新檔 `1.6/Defs/Hediffs/Hediff_ERAPlating.xml`

* **`DMS_Hediff_ERAPlating`** — `Fortified.HediffCompProperties_DamageBlocker`（框架 `FFF_DamageBlocker_HediffExample` 的實作）
  * `damageThreshold 18`、`blockCharges 4`、`preApplyDamagePriority 20`（掛最外層，先於護盾判定）
  * 白名單 `Bomb` + `Bomb_Secondary`(CE)、`allowMelee false` → 只擋聚能裝藥與爆裂物
  * 高閾值：耗 1 塊 + 傷害歸零；低閾值：不耗不擋，交回本體裝甲
  * `eraOnConsumeCharge` → 引爆回震本體 6 點鈍傷
  * 鋼材手動整備（`requiredReplenishWorkType: Crafting`）
* **`DMS_HediffGiverSet_ERAPlating`** — `Fortified.HediffGiver_RandomSilent`，`mtbDays 0.0001`，由壁虎的 `race.hediffGiverSets` 掛上

實作前用反編譯確認過兩件事：`Pawn_HealthTracker.HealthTickInterval` 跑 `hediffGiverSets` 時不做 `IsFlesh` 過濾（機兵吃得到），`HediffGiverUtility.TryApply` 對無 `partsToAffect` 的全身 hediff 自帶 `HasHediff` 去重（不會重複施加）。因此純 XML 即可運作，無需改 C#。

敘述改為「削薄基礎裝甲、把重量讓給外掛反應裝甲」，Sharp 0.9 < 鬣蜥 1.4 從矛盾變成設定的一部分。

### A2 鬣蜥移速
`MoveSpeed 3.5 → 4.6`。現在 鬣蜥 4.6 > 壁虎 4.3 > 駭蜥 4.0，英文敘述的 "greater mobility" 成立。

### A3 先鋒 — 改敘述
拿掉「改進的瞄準感測器／更好的能力」（`ShootingAccuracyPawn` 與 `AimingDelayFactor` 其實與標準型相同、技能等級還更低），改為強調武器相容性最廣、能脫離補給線獨立行動 —— 這是它武器標籤上真正的差異。

### A4 方陣步兵 — 改英文敘述
原文沿用浪人的 "assault armor and high mobility package"，實際四項防護／機動全面低於浪人。改為 cost-reduced 量產型，並明說「比浪人更慢更薄皮、靠數量鋪開」。中英現已一致。

### A5 獒犬 — 統一中英
英文的 "urban combat support" 改為與中文一致的「低車身、開闊地機動火力支援」，並補上人工肌肉束的說明。

### A6 游隼 — 改英文敘述
"heavy-duty" → "light"（`mechWeightClass Light`、bodySize 0.5、頻寬 1）；並把 "working in conjunction with anti-tank units" 改為自行執行反裝甲打擊，對應 `BypassUsableWeapons` 裡的 `CE_ATFour`。

---

## B. 敘述有宣稱、數值無對應

| 項目 | 修正 |
|---|---|
| **B1 士兵系列** | `BaseMechRace_Soldat` 加 `MechEnergyUsageFactor 0.7`，支撐「較長的電池壽命」 |
| **B2 戰鬥夥伴** | 加 `CarryingCapacity 110`，支撐「相當程度的負載能力」 |
| **B3 運輸框架** | 加 `CarryingCapacity 190`（系列最高）；`WastepacksPerRecharge 4 → 2`（後勤機不該比戰鬥機更耗損）；敘述補上載重優勢 |
| **B4 寄居蟹** | 敘述補上磁穩場兼作短距彈道攔截屏障，對應原本隱藏的 `CompProperties_ProjectileInterceptor` |

> **實作註記**：原本打算給運輸框架加 `CaravanBonusSpeedFactor`，反編譯後發現 `CaravanBonusUtility.HasCaravanBonus()` 要求 `RaceProps.Humanlike`，對 ToolUser 機兵完全無效，因此改用 `CarryingCapacity`。
> **順帶發現**：小猛瑪身上的 `CaravanBonusSpeedFactor 6` 因為同一個原因是死值 —— 它的商隊優勢實際只來自 `CarryingCapacity 200`。未指派，未改動。

---

## C. 家族內部一致性

### C1 蠵龜繼承
* `ParentName` `BaseAutomatroidRace` → `BaseGeochelone`，刪除重複的 statBases / race / tools / butcherProducts / damageMultipliers 與重複的 Trample / Panic comps
* 現正確繼承 `CompProperties_ProjectileInterceptor` 與 `MechEnergyUsageFactor 0.5`
* **連帶修正 CE patch**：`CE/Patches/Pawns/Automatroid_Heavy_Geochelones.xml` 中所有 ThingDef xpath 移除 `defName="DMS_Mech_Caretta" or`。RimWorld 的 PatchOperation 在繼承解析**之前**執行，不改的話 CE 的 `ArmorDurability`、`RacePropertiesExtensionCE`、`PartialArmorExt` 會被套用兩次。PawnKindDef 的 Caretta xpath 保留不動。
* 副作用：蠵龜現在也會掉 `Neurocomputer` 與 `detritusLeavings`（與象龜同底盤，合理）

### C2 工程駁機 — 改敘述
承認它更高大、作業防護板更厚，並解釋 `BandwidthCost 4 > 戰鬥夥伴 3` 是測繪與開挖運算的代價；「成本簡化」限定在武器系統上。

### C5 格鬥士 — 改英文敘述
"maintains the Ronin's mobility and armor" → 保留機動性，但**以正面裝甲換取**靈活結構與鈍擊防護（對應 Sharp 1.0→0.9、Heat 0.67→0.5、Blunt 0.7→0.8）。

### C7 突擊龜 — 改敘述
移除「小型」，改為承認車體規模超過原型象龜（bodySize 3.3 / health 5.3）。

### C8 移除 _b 變體
* 刪 `DMS_Mech_Geochelone_b` / `DMS_Mech_Killdozer_b` 的 ThingDef + PawnKindDef
* 三語 DefInjected 對應條目全部清除，全 mod grep 已無殘留
* `CE/Patches/Weapons/MiliGunsMounted_CE_Replace.xml`：刪掉 Geochelone_b 那條；Killdozer_b 那條**改指向存活的 `DMS_Mech_Killdozer` 的 `CompProperties_VehicleWeapon/defaultWeapon`** —— 照原樣刪掉的話，CE 下推土機的 Kord 就不會被換成 `DMS_Dozer_KordMG`

---

## 中文化同步

繁中 + 簡中 DefInjected 已同步改寫：鬣蜥、壁虎、蠵龜、突擊龜、先鋒、方陣步兵、格鬥士、寄居蟹、工程駁機、運輸框架。
獒犬與游隼的中文原本就是正確的一方，未改動（是英文向中文靠攏）。

---

---

## D. 框架層修復：CaravanBonusSpeedFactor（追加）

**問題**：原版 `CaravanBonusUtility.HasCaravanBonus()` 把商隊速度加成鎖在 `RaceProps.Humanlike`：

```csharp
if (pawn.RaceProps.Humanlike && !pawn.Downed)
    return pawn.GetStatValue(StatDefOf.CaravanBonusSpeedFactor) > 1f;
return false;
```

任何 ToolUser 機體即使在 `statBases` 填了這個 stat 也完全無效，是個沉默的死值。

**修復**：新檔 `_Fortified-Framework/_Sources/Fortified/Patch/Patch_CaravanBonusUtility.cs`

* `[HarmonyPatch(typeof(CaravanBonusUtility), nameof(CaravanBonusUtility.HasCaravanBonus))]` + postfix
* 條件：非 Humanlike（人形交還原版邏輯）、未死亡未倒地、`CaravanBonusSpeedFactor > 1`
* 該 stat 的 `defaultBaseValue` 就是 1，因此等同「有填才生效」，不會誤傷任何沒主動設定的 race
* 走 `PatchAll()` 自動掛載；csproj 是 SDK-style 預設 glob，新檔自動納入編譯

**連帶數值調整**（因為原版取的是「有加成成員的平均值」而非全隊平均，單一高值會直接放大整支商隊速度）：

| 機體 | 原值 | 新值 | 理由 |
|---|---|---|---|
| 小猛瑪 | 6 | **1.5** | 專職運輸車，+50% 已很有存在感 |
| 運輸框架 | — | **1.25** | 後勤改裝型，定位在小猛瑪之下 |
| 戰鬥夥伴 | — | **1.15** | 可乘騎的步兵支援機，載重 110，梯度最低階 |

三台的共通點是都有 `CarryingCapacity`，其餘機兵（中繼指揮、工程駁機等）不給。

**兩個 caravan stat 的差異**（實測前值得知道）：

| | `CaravanRidingSpeedFactor` | `CaravanBonusSpeedFactor` |
|---|---|---|
| 前置判定 | `IsCaravanRideable()` — 需 `LifeStageDef.caravanRideable` | `HasCaravanBonus()` — 原版限 Humanlike，**已由框架 patch 放寬** |
| DMS 機兵可用? | ✅ 兩個 LifeStageDef 都是 `caravanRideable true` | ✅ patch 後可用 |
| 需要隊上有人類? | ✅ 需要（可乘騎數上限 = 人類數） | ❌ 不需要，純機兵商隊也吃得到 |
| 計算方式 | 取平均後與 bonus **各自獨立相乘** | 同左 |

戰鬥夥伴同時有兩個（riding 1.3 + bonus 1.15），在 `CaravanTicksPerMoveUtility.GetTicksPerMove()` 中會相乘 —— 這是刻意的（既能被騎、又提供後勤支援），但不算直觀，故在 XML 註解裡標明。

### 加掛貨箱改造（`DMS_BF_ExtendCargo`）

在既有的 `statOffsets` 加上 `CaravanBonusSpeedFactor +0.15`（加算）。裝上後：

| 機體 | 基礎 | 裝貨箱後 |
|---|---|---|
| 運輸框架 | 1.25 | **1.40** |
| 戰鬥夥伴 | 1.15 | **1.30** |
| 中繼指揮 | 1.00 | **1.15** |
| 鬣蜥 | 1.00 | **1.15** |

四台都仍低於專職運輸車小猛瑪的 1.5。中繼指揮與鬣蜥本來沒有商隊加成，裝上貨箱後才跨過 `> 1` 的門檻開始生效 —— 這正是這個改造件應有的效果。

該 hediff 原本就有 `MoveSpeed ×0.8`：地圖上因負重變慢、世界地圖上因載運能力變快，兩者不衝突（`GetTicksPerMove()` 看的是載重與 bonus，不是步速）。英文敘述已補上這層說明；中文敘述原本就寫了「用來提高運輸效率」，不需改。

**已知未修**：`TransferableUIUtility.DoExtraIcons()` 中 `else if (ModsConfig.BiotechActive && pawn.IsColonyMech)` 分支排在 `HasCaravanBonus` 之前，所以殖民地機兵在商隊介面上不會顯示加成圖示。純外觀問題，速度計算不受影響，已寫進 patch 檔註解。

> ⚠️ **此項需重新編譯 `Fortified.dll`**（跑 `_Sources/Fortified/build.bat`）。本次環境無 dotnet，只做了型別／方法簽章對照與語法平衡檢查，未實際編譯。

---

## E. 重型機兵量級壓迫光環（追加）

原本只有 UltraHeavy 五台有 `Fortified.CompProperties_CausePanicInRange`，Heavy 級全部沒有。

### 新增對象（僅戰鬥用重型機）
鬣蜥、壁虎、猿人。**排除**工程駁機、小猛瑪、運輸框架、中繼指揮、戰鬥夥伴 —— 後勤／非戰鬥機不製造壓迫感，且體型 1.3 的人形戰鬥框架與中型機兵外型差異不足。

### 啟用內建體型差修正
全部八台（含既有 UltraHeavy）補上 `useBodySizeModifier` + `bodySizeCurve`，讓「本機體型 ÷ 目標體型」再乘一層。這是 comp 原本就設計好的用途，效果會隨目標體型自動衰減。

共用曲線：

| 體型比 | 0.5 | 1.0 | 1.75 | 2.5 | 3.5 | 5.0 |
|---|---|---|---|---|---|---|
| 倍率 | 0.50 | 1.00 | 1.25 | 1.50 | 1.85 | 2.00 |

### 完整梯度（對體型 1.0 人類的實際每次觸發率）

| 機體 | 體型 | range | tick | base | **實際** |
|---|---|---|---|---|---|
| 駭蜥 | 3.5 | 9 | 200 | 0.25 | **0.463** |
| 突擊龜 | 3.3 | 7 | 250 | 0.20 | **0.356** |
| 象龜／蠵龜 | 3.0 | 7 | 300 | 0.15 | **0.251** |
| 寄居蟹 | 3.0 | 4 | 400 | 0.07 | **0.117** |
| 鬣蜥／壁虎 | 2.5 | 5 | 350 | 0.10 | **0.150** |
| 猿人 | 1.75 | 4 | 400 | 0.06 | **0.075** |

寄居蟹刻意低於同體型的象龜（工業母機而非兵器），保留原設計意圖。

### 機制備註
* comp 只掃描 `attackTargetsCache` 的**敵對** pawn，不會影響己方
* 預設心理狀態 `FFF_FleeInPlace`，可被 `FFF_FearResistance` stat 抵抗
* 已在心理狀態中的 pawn 不會重複觸發；施加者被暈眩時效果暫停

---

## F. 士兵系列「原地封存」技能（追加）

框架其實**已經有完整的機兵休眠容器系統**（`MechCapsuleUtility` / `Building_MechCapsule` / `FFF_MechCapsule_*`），但只提供 `CompMechDeactivate` 這個常駐 Gizmo 版本。這次補上 AbilityDef 版本。

### 框架端
新檔 `_Sources/Fortified/Ability/CompAbilityEffect_DeactivateToCapsule.cs`

* `CompAbilityEffect_DeactivateToCapsule` + `CompProperties_AbilityDeactivateToCapsule`
* 收納流程共用既有的 `MechCapsuleUtility.DeactivateMech()`，不重複造輪子
* 覆寫 `AbilityComp.CanCast` 與 `AbilityComp.GizmoDisabled(out string)`，滑鼠移上去會顯示不可用的具體原因
* 可設參數：`playerOnly`、`allowWhileDrafted`、`allowWhileDowned`、`sendMessage`、`effecterOnDeactivate`
* 新增 6 個 `FFF.Mothball.Reason.*` 翻譯鍵到 `Languages/*/Keyed/MechCapsule.xml`（英／繁／簡）

### DMS 端
* `Defs/Abilities/Common.xml` 新增 `DMS_Mothball`（field mothball／原地封存）
  * 暖機 3 秒、冷卻 2500 ticks、`aiCanUse false`、`targetable false`
  * 預設禁止徵召狀態下使用（戰鬥中封存等同棄械）
* 掛給 5 個 PawnKindDef：士兵、先鋒隊(Soldat_B)、擲彈兵、炮兵、軍士
  * **不含** `DMS_Mech_Raider`（海盜特供型，非玩家單位）
* 三語 DefInjected 已補

### 圖標
新檔 `Textures/UI/Ability/Mothball.png`（128×128 RGBA）

以 PIL 程式化繪製，沿用 DMS 既有 ability 圖標的視覺語彙：深色描邊、軍綠灰主體、綠色重點色（取自 `Retract.png`）。構圖為掀蓋的軍規貨箱 + 折疊收納中的機兵剪影 + 向下收納箭頭。

### 機制備註
* 士兵體型 1.1 → `GetMechSize()` 取 drawSize 後映射到 `FFF_MechCapsule_Small`
* 封存時會解除 overseer 關係，因此不再佔用頻寬；重新啟動需由機械師執行（框架既有的右鍵選單流程）
* ⚠️ **需重新編譯 `Fortified.dll`**（與 D 項的 caravan patch 同一次即可）

---

## 上線前建議實測

1. 生成一台壁虎 → 健康頁應出現 "electromagnetic reactive plating"，左下角有充能 Gizmo（4/4）
2. 用火箭筒／AT4 打它 → 顯示 `FFF.DamageBlocker.Absorbed`、塊數 −1、本體吃 6 點鈍傷
3. 用步槍打它 → **不**消耗塊數（低閾值放行，走本體裝甲）
4. 右鍵壁虎 → 應出現「整備 (鋼材 xN)」
5. 開 CE 讀檔 → 確認蠵龜沒有重複的 `CompProperties_ArmorDurability` 紅字
6. 確認蠵龜的護盾 Gizmo 有出現（C1 的主要目的）
7. 重編 `Fortified.dll` 後組一支含小猛瑪的商隊 → 世界地圖速度說明應出現
   「MultiplierFromMoveSpeedBonus: 150%」；把小猛瑪換成運輸框架應為 125%
8. 選取一台士兵 → 技能列應出現「原地封存」（貨箱圖標）；徵召後圖標應變灰並顯示
   「徵召狀態下無法封存」
9. 未徵召時施放 → 3 秒暖機後原地變成 `FFF_MechCapsule_Small`，右鍵該建築應可由
   機械師重新啟動

## 未指派、留待日後判斷

* **C3** 三種中繼機的頻寬漲幅無共通規則（士兵 1→4、戰鬥夥伴 3→4、游隼 1→3）
* **C4** 火槍手家族熱抗無規律（0.67 / 0.67 / 0.50 / 0.83）
* **C6** 襲城者「頂級裝甲」Sharp 1.25 仍低於同系列塔盾手的 1.40
* **士兵系列**「無法在沒有掩護的情況下進行戰鬥」仍無機制對應 —— `DMS_Battery` 在 Soldat 身上 coverage 0.02 / Inside，比浪人用的 TypeB（0.04）**更難**被打中。要落實需改 BodyDef
* **運輸框架不可乘騎**：`CaravanRidingSpeedFactor 1.3` 只寫在 `DMS_Mech_BattleFrame` 這個 def 上，不在 `BaseBattleFrame`，所以運輸框架沒有繼承到。若希望運輸型也能乘騎需另外補
